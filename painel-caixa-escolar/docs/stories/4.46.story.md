# Story 4.46 — Remover coluna "Un." da tabela Pré-Orçamentos Salvos

**Epic:** 4 — Melhorias GDP
**Status:** Ready
**Priority:** P3 — Baixa (cleanup UI)
**Depends on:** —

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [manual-review, browser]
```

---

## Story

**As a** operador que consulta pré-orçamentos salvos no Intel Preços,
**I want** que a coluna "Un." seja removida da tabela,
**so that** a tabela fique mais limpa e sem informação redundante.

---

## Acceptance Criteria

### AC1 — Remover coluna "Un." do header
- [ ] Remover `<th>Un.</th>` da tabela de pré-orçamentos salvos em `index.html` (linha ~386)

### AC2 — Remover coluna "Un." do render
- [ ] Remover o `<td>` correspondente no `renderPreOrcamentos()` em `app.js` (linha ~2056)
- [ ] O `<td>` que mapeia `p.itens` para extrair unidades deve ser removido inteiramente

---

## File List

| File | Action | Description |
|------|--------|-------------|
| `squads/caixa-escolar/dashboard/index.html` | MODIFY | Remover th da coluna Un. |
| `squads/caixa-escolar/dashboard/app.js` | MODIFY | Remover td do render |
