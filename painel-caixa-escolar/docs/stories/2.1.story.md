# Story 2.1 — Remocao de Credenciais e Dados Sensiveis

**Epic:** 2 — Resolucao de Debito Tecnico
**Sprint:** 0 (Semana 1)
**Prioridade:** P0 — IMEDIATO
**Estimativa:** 10.5 horas
**Debitos resolvidos:** D-01, D-02, D-04, D-19, SEC-5, SEC-1 (parcial)

---

## Descricao

Remover todas as credenciais, senhas e dados pessoais identificaveis (PII) que estao commitados no repositorio. Isso inclui limpar o historico do Git para garantir que dados sensiveis nao possam ser recuperados de commits anteriores. Esta e a acao mais urgente — credenciais de escolas publicas estao expostas.

**Pre-requisito de negocio:** Comunicar as escolas sobre a rotacao de credenciais ANTES de iniciar esta story.

---

## Criterios de Aceitacao

- [ ] `escolas-credentials.json` removido do repositorio e do historico Git
- [ ] Comentario com senha `lariucci2026` removido de `auth.js`
- [ ] CNPJ real substituido por placeholder no `.env.example`
- [ ] Dados PII (nomes, telefones, emails de responsaveis) removidos do historico Git
- [ ] Credenciais de escolas migradas para `.env` ou arquivo nao-commitado
- [ ] `.gitignore` atualizado para prevenir re-commit de arquivos sensiveis
- [ ] Verificacao: `grep -r "lariucci\|gdp2025\|escola2025\|21.345.678" .` retorna zero
- [ ] Verificacao: `git log --all --full-history -- escolas-credentials.json` mostra arquivo ausente

---

## Tarefas

- [ ] Comunicar escolas sobre rotacao de credenciais (coordenar com PM)
- [ ] Instalar `git-filter-repo` (mais seguro que `git filter-branch`)
- [ ] Executar `git-filter-repo` para remover `escolas-credentials.json` do historico
- [ ] Executar `git-filter-repo` para remover PII de commits anteriores
- [ ] Remover comentario com senha de `dashboard/auth.js`
- [ ] Substituir CNPJ real por `00.000.000/0001-00` no `.env.example`
- [ ] Criar `.env.credentials` template para credenciais de escolas
- [ ] Atualizar `.gitignore` com padroes de arquivos sensiveis
- [ ] Implementar lock file (`scan.lock`) para prevenir concorrencia (D-23)
- [ ] Testar que sistema funciona apos remocao dos arquivos
- [ ] Forcar `git push --force` para atualizar remote (coordenar com equipe)
- [ ] Notificar todos para fazer `git clone` fresco apos limpeza

---

## Dependencias

- **Nenhuma dependencia tecnica** — pode ser feito primeiro
- **Dependencia de negocio:** Comunicacao com escolas antes de rotacionar credenciais

## Riscos

- `git-filter-repo --force` reescreve historico — todos precisam fazer clone fresco
- Se houver forks ou PRs abertos, serao invalidados

## Arquivos Afetados

- `squads/caixa-escolar/dashboard/data/escolas-credentials.json` (remover)
- `squads/caixa-escolar/dashboard/auth.js` (editar)
- `squads/caixa-escolar/.env.example` (editar)
- `.gitignore` (editar)
- `squads/caixa-escolar/server.js` (adicionar lock file)

---

*Story criada por @pm (Morgan)*
