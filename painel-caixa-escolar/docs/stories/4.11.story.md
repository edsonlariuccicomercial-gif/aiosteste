# Story 4.11 — NCM Inteligente: classificação automática com IA + auto-preenchimento no import

**Epic:** 4 — Melhorias GDP
**Status:** Approved
**Priority:** High
**Estimated Complexity:** Medium

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [manual-review]
```

---

## Story

**As a** fornecedor que importa contratos com dezenas de itens alimentícios, de limpeza e escolares,
**I want** que o sistema classifique automaticamente o NCM de cada produto usando IA quando o mapa local não encontrar,
**so that** nenhum item fica sem NCM e as notas fiscais são emitidas corretamente no Olist/Tiny.

---

## Design Decision

**Hierarquia de fallback para NCM (3 camadas):**

1. **Mapa local expandido** (144 keywords) — instantâneo, sem custo
2. **Banco de Produtos** — busca produto similar que já tenha NCM
3. **IA (Claude API)** — classifica qualquer produto por descrição, mesmo inédito

A IA é o último recurso mas o mais poderoso — entende sinônimos, variações regionais e especificações técnicas.

---

## Acceptance Criteria

### AC-1: Unificar mapa de NCM do frontend com os backends
- [ ] Copiar as entradas que faltam no `TINY_NCM_MAP` do `gdp-contratos.html` (74 entradas) para igualar ao `NCM_MAP` do `tiny-produtos.js` (144 entradas)
- [ ] Categorias a adicionar: hortifruti (26), carnes/proteínas (15), laticínios (7), temperos (6), grãos (8), descartáveis, higiene
- [ ] Garantir que `findNcmLocal()` no frontend agora cobre as mesmas categorias que os backends

### AC-2: Auto-preenchimento de NCM ao importar contrato
- [ ] Após cada fluxo de criação de contrato (import mapa, lista, cronograma, manual), rodar `findNcmLocal()` para CADA item com NCM vazio
- [ ] Preencher automaticamente o NCM no item do contrato
- [ ] Exibir toast: "NCM auto-preenchido: X/Y itens"
- [ ] Para itens que o mapa local não encontrou: marcar para lookup via IA (AC-4)

### AC-3: Fallback — buscar NCM no Banco de Produtos
- [ ] Se `findNcmLocal()` retorna null, buscar no Banco de Produtos (`gdp.produtos.v1`) por descrição similar (mesmo match usado no enrichment de SKU)
- [ ] Se encontrar produto com NCM preenchido no Banco, herdar o NCM
- [ ] Isso cobre produtos que já foram classificados anteriormente por qualquer meio

### AC-4: Fallback IA — endpoint de classificação NCM com Claude
- [ ] Criar serverless function `api/ai-ncm.js` que recebe uma lista de descrições e retorna NCMs classificados via Claude API
- [ ] Prompt da IA: recebe descrições de produtos, retorna JSON com NCM + justificativa para cada item
- [ ] Rate limit: processar em batch (até 20 itens por chamada) para eficiência
- [ ] Cache: salvar resultado da IA no Banco de Produtos para não reclassificar o mesmo produto

### AC-5: Integração no frontend — botão "Classificar NCM com IA"
- [ ] Nos detalhes do contrato: ao lado de "Preencher NCM Todos", adicionar botão **"🤖 Classificar com IA"**
- [ ] Ao clicar: coleta itens sem NCM → chama `api/ai-ncm.js` → preenche resultados → salva no contrato + Banco
- [ ] Toast de progresso: "Classificando X itens com IA..." → "X NCMs classificados com sucesso"
- [ ] Se IA falhar: toast de erro, itens ficam sem NCM (não bloqueia)

### AC-6: Auto-classificação no import (pipeline completa)
- [ ] Após importar contrato, executar pipeline automática:
  1. `findNcmLocal()` para todos os itens (mapa expandido)
  2. Para itens sem NCM: buscar no Banco de Produtos
  3. Para itens restantes sem NCM: chamar `api/ai-ncm.js` em background
- [ ] Pipeline é assíncrona (não bloqueia criação do contrato)
- [ ] Exibir badge no contrato: "X itens sem NCM" se pipeline ainda não completou

---

## Tasks / Subtasks

### Task 1: Expandir TINY_NCM_MAP no frontend (AC-1)

- [ ] 1.1 Copiar as entradas de `tiny-produtos.js` NCM_MAP (linhas 80-158) que faltam no `gdp-contratos.html` TINY_NCM_MAP
- [ ] 1.2 Categorias a adicionar:
  - Hortifruti: alface, tomate, cebola, cebolinha, couve, repolho, batata, cenoura, beterraba, chuchu, abobora, pimentao, quiabo, jilo, mandioca, inhame, banana, laranja, limao, maca, mamao, melancia, abacaxi, manga, alho, cheiro verde, milho espiga
  - Carnes: frango, carne bovina, carne suina, linguica, salsicha, ovo, peixe
  - Laticínios: queijo, requeijao, iogurte, creme de leite, leite condensado, leite em po
  - Temperos: colorau, pimenta, oregano, louro, cominho, caldo, mostarda, maionese, ketchup
  - Grãos: aveia, granola, ervilha, milho conserva, lentilha, soja
  - Descartáveis/Higiene: prato descartavel, talher, marmitex, filme pvc, papel aluminio, shampoo, creme dental, escova dental, fralda, absorvente
- [ ] 1.3 Validar que `findNcmLocal()` agora encontra "maçã", "frango", "queijo", etc.

### Task 2: Auto-preencher NCM após import (AC-2, AC-3)

- [ ] 2.1 Criar função `autoPreencherNcm(contratoId)`:
  ```javascript
  function autoPreencherNcm(contratoId) {
    const c = contratos.find(x => x.id === contratoId);
    if (!c) return { filled: 0, pending: 0 };
    loadBancoProdutos();
    const bp = bancoProdutos.itens || [];
    let filled = 0, pending = 0;
    c.itens.forEach(item => {
      if (item.ncm && item.ncm.length >= 8) return; // já tem NCM
      // Camada 1: mapa local
      const local = findNcmLocal(item.descricao);
      if (local) { item.ncm = local.ncm; filled++; return; }
      // Camada 2: Banco de Produtos
      const descNorm = (item.descricao || '').toUpperCase().trim();
      const bpMatch = bp.find(p => (p.descricao || '').toUpperCase().trim() === descNorm && p.ncm);
      if (bpMatch) { item.ncm = bpMatch.ncm; filled++; return; }
      pending++;
    });
    if (filled > 0) saveContratos();
    return { filled, pending };
  }
  ```
- [ ] 2.2 Chamar `autoPreencherNcm(contrato.id)` após cada fluxo de criação de contrato (mesmos 4 call sites da story 4.9)
- [ ] 2.3 Toast: `"NCM auto-preenchido: ${filled}/${total} itens. ${pending} pendente(s) de IA."`

### Task 3: Criar endpoint `api/ai-ncm.js` (AC-4)

- [ ] 3.1 Criar serverless function:
  ```javascript
  // POST /api/ai-ncm
  // Body: { items: [{ descricao: "Maçã fuji tipo exportação 1kg" }, ...] }
  // Response: { success: true, results: [{ descricao: "...", ncm: "0808.10.00", justificativa: "Maçãs frescas" }] }
  export default async function handler(req, res) {
    const { items } = req.body;
    if (!items || !items.length) return res.status(400).json({ error: "items[] obrigatório" });

    const prompt = `Você é um classificador fiscal brasileiro especialista em NCM (Nomenclatura Comum do Mercosul).
Para cada produto abaixo, retorne o código NCM mais adequado (formato XXXX.XX.XX) e uma justificativa breve.

Produtos:
${items.map((it, i) => `${i+1}. ${it.descricao}`).join('\n')}

Responda APENAS em JSON:
[{"idx": 1, "ncm": "XXXX.XX.XX", "justificativa": "..."}, ...]`;

    const resp = await fetch("https://api.anthropic.com/v1/messages", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "x-api-key": process.env.ANTHROPIC_API_KEY,
        "anthropic-version": "2023-06-01"
      },
      body: JSON.stringify({
        model: "claude-haiku-4-5-20251001",
        max_tokens: 2048,
        messages: [{ role: "user", content: prompt }]
      })
    });
    // Parse response, extract JSON, return results
  }
  ```
- [ ] 3.2 Configurar `ANTHROPIC_API_KEY` como variável de ambiente no Vercel
- [ ] 3.3 Usar model `claude-haiku-4-5-20251001` (rápido e barato para classificação)
- [ ] 3.4 Batch: aceitar até 20 itens por chamada

### Task 4: Botão "Classificar com IA" no frontend (AC-5)

- [ ] 4.1 Nos detalhes do contrato, ao lado de "Preencher NCM Todos", adicionar:
  ```html
  <button class="btn btn-sm" style="background:rgba(139,92,246,.15);color:var(--purple);border:none;font-weight:700"
    onclick="classificarNcmIA('${c.id}')">🤖 Classificar com IA</button>
  ```
- [ ] 4.2 Implementar `classificarNcmIA(contratoId)`:
  ```javascript
  async function classificarNcmIA(contratoId) {
    const c = contratos.find(x => x.id === contratoId);
    if (!c) return;
    const semNcm = c.itens.filter(i => !i.ncm || i.ncm.length < 8);
    if (semNcm.length === 0) { showToast("Todos os itens já têm NCM!"); return; }
    showToast(`Classificando ${semNcm.length} itens com IA...`, 3000);

    // Batch em grupos de 20
    for (let start = 0; start < semNcm.length; start += 20) {
      const batch = semNcm.slice(start, start + 20);
      try {
        const resp = await fetch("/api/ai-ncm", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ items: batch.map(i => ({ descricao: i.descricao })) })
        });
        const data = await resp.json();
        if (data.success && data.results) {
          data.results.forEach((r, idx) => {
            if (r.ncm && batch[idx]) {
              batch[idx].ncm = r.ncm;
              // Salvar no Banco de Produtos para cache
              adicionarAoBancoProdutos({ ...batch[idx], ncm: r.ncm });
            }
          });
        }
      } catch(err) {
        showToast("Erro na classificação IA: " + err.message, 4000);
      }
    }
    saveContratos();
    abrirContrato(contratoId);
    showToast(`IA classificou ${semNcm.length} itens!`, 4000);
  }
  ```

### Task 5: Pipeline automática no import (AC-6)

- [ ] 5.1 Após cada import de contrato (mesmos call sites), executar pipeline:
  ```javascript
  // Após autoCadastrarTiny(contrato.id):
  const { filled, pending } = autoPreencherNcm(contrato.id);
  if (pending > 0) {
    // Background: classificar com IA os restantes
    classificarNcmIA(contrato.id);
  }
  ```
- [ ] 5.2 Pipeline é não-bloqueante (usa async/await em background)

### Task 6: Teste manual

- [ ] 6.1 Importar mapa com produtos variados (frutas, carnes, limpeza) → verificar que NCM é preenchido automaticamente para maioria
- [ ] 6.2 Produtos inéditos (ex: "Quinoa orgânica 500g") → verificar que IA classifica corretamente
- [ ] 6.3 Clicar "🤖 Classificar com IA" em contrato com itens sem NCM → NCMs preenchidos
- [ ] 6.4 Verificar que NCM classificado pela IA é salvo no Banco de Produtos (cache)
- [ ] 6.5 Re-importar mesmo produto → NCM vem do Banco de Produtos (sem chamar IA novamente)

---

## Dev Notes

### Mapa de NCM expandido — fonte
Copiar entradas das linhas 80-158 de `api/tiny-produtos.js` para `TINY_NCM_MAP` em `gdp-contratos.html`.

### API de IA — custo estimado
- GPT-4o-mini: ~$0.001 por batch de 20 itens
- Contrato típico: 15-40 itens → 1-2 chamadas → ~$0.002 por contrato
- Cache no Banco de Produtos elimina chamadas repetidas

### Variável de ambiente necessária
- `OPENAI_API_KEY` — já configurada no Vercel (criada há 5 dias)

### Source Tree
```
squads/caixa-escolar/dashboard/
├── gdp-contratos.html    ← MODIFY (expandir NCM_MAP, autoPreencherNcm, classificarNcmIA, pipeline)
└── api/
    └── ai-ncm.js         ← CREATE (endpoint de classificação NCM com Claude)
```

---

## Testing

| # | Cenário | Resultado esperado |
|---|---------|-------------------|
| T1 | Import mapa com "Maçã Fuji 1kg" | NCM auto-preenchido via mapa local expandido |
| T2 | Import com "Frango coxa e sobrecoxa" | NCM auto-preenchido via mapa local |
| T3 | Import com "Quinoa orgânica 500g" | NCM não encontrado localmente → IA classifica |
| T4 | Botão "🤖 Classificar com IA" | Itens sem NCM classificados em batch |
| T5 | Mesmo produto importado novamente | NCM vem do Banco de Produtos (cache), sem IA |
| T6 | API Anthropic offline | Toast de erro, itens ficam sem NCM (não bloqueia) |
| T7 | Contrato com 40 itens sem NCM | 2 batches de 20 → todos classificados |

---

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled

---

## Out of Scope

- Consulta à API do SISCOMEX/Receita Federal (complexa, requer certificado digital)
- Edição manual de NCM em massa (já existe no "Editar Selecionados")
- Validação de NCM contra tabela oficial (futuro)
- Classificação fiscal para operações internacionais (fora do escopo caixa escolar)

---

## File List

| File | Action |
|------|--------|
| `squads/caixa-escolar/dashboard/gdp-contratos.html` | MODIFY — expandir NCM_MAP, autoPreencherNcm, classificarNcmIA, pipeline |
| `squads/caixa-escolar/dashboard/api/ai-ncm.js` | CREATE — endpoint de classificação NCM com Claude Haiku |

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-17 | 1.0 | Draft — NCM inteligente com IA + auto-preenchimento + pipeline 3 camadas | @sm (River) |
