# Story 6.2 — Hook NF Entrada → Atualização Automática de Custo no Banco

**Epic:** Epic 6 — Inteligência de Preços: Fechamento de Loops
**Status:** Done
**Priority:** P0
**Story Points:** 2

---

## Problema

As funções `registrarNotaEntradaManual()`, `importarNotaEntradaXml()` e `consultarNotasEntradaApi()` em `gdp-init.js` já fazem matching de itens NF por NCM e atualizam `custoBase` no `bancoPrecos` local. Porém:

1. A atualização fica **apenas em localStorage** — não persiste em Supabase
2. O `build-price-history.js` não consome dados de NF entrada — o histórico de custos não reflete compras reais
3. Não há registro de tipo "nf_entrada" no histórico de preços

## Scope

### IN
- Persistir atualização de custo (NF entrada) em Supabase `preco_historico` com tipo="nf_entrada"
- Criar tabela `preco_historico` em migration se não existir
- Enriquecer `build-price-history.js` para incluir registros de NF entrada no histórico
- Toast visual confirmando "Custo atualizado: ITEM → R$ X,XX (fonte: NF entrada)"

### OUT
- Mudança na lógica de matching NCM (já funciona)
- Import de NF via SEFAZ DistribuicaoDFe (já funciona via `consultarNotasEntradaApi`)

---

## Acceptance Criteria

### AC-1: Tabela preco_historico em Supabase
- [x] Criar `005_preco_historico.sql` com tabela:
  - `id`, `empresa_id`, `sku`, `escola`, `sre`
  - `tipo` CHECK (proposta, ganho, perdido, contrato, nf_saida, nf_entrada)
  - `valor`, `custo_base`, `margem_pct`, `fonte`
  - `metadata` (JSONB — dados extras como NCM, fornecedor, número NF)
  - `created_at`
  - RLS: empresa_id = auth.uid()
- [x] Índices: `empresa_id`, `sku`, `tipo`, `created_at`

### AC-2: Persistir custo de NF entrada em Supabase
- [x] Nas 3 funções de NF entrada (`registrarNotaEntradaManual`, `importarNotaEntradaXml`, `consultarNotasEntradaApi`), após atualizar custoBase local:
  - Inserir registro em `preco_historico` com tipo="nf_entrada"
  - Incluir: sku, valor=valorUnitario, custo_base=valorUnitario, fonte="nf_entrada", metadata={fornecedor, nf_numero, ncm}
- [x] Graceful degradation: se Supabase falhar, não bloquear fluxo
- [x] Se múltiplas NFs para mesmo item/SKU, custoBase = valor da NF mais recente (último preço pago)

### AC-3: Toast de confirmação visual
- [x] Quando custoBase é atualizado via NF entrada, mostrar toast:
  - "✅ Custo atualizado: [item] → R$ X,XX (NF #YYYY de [fornecedor])"
- [x] Se matching falhar (item não encontrado no banco), mostrar toast amarelo:
  - "⚠️ Item NF sem match no banco: [descricao] (NCM: XXXXX)"

### AC-4: Verificação de persistência
- [x] Após inserir em Supabase, verificar que registro existe com query select
- [x] Log no console: "preco_historico: X registros inseridos para NF #YYYY"

---

## Technical Notes

- NF Entrada funções em `gdp-init.js` linhas 1597-1839
- Bridge 1 (matching NCM) já implementado nessas funções
- `build-price-history.js` em `painel-caixa-escolar/scripts/` (146 linhas)
- O matching existente usa: `bp.itens.find(i => i.ncm === item.ncm && normalize(i.item).includes(normalize(item.descricao)))`

## File List

- [x] `painel-caixa-escolar/supabase/migrations/005_preco_historico.sql`
- [x] `painel-caixa-escolar/squads/caixa-escolar/dashboard/js/gdp-init.js`
- [x] `painel-caixa-escolar/scripts/build-price-history.js`
