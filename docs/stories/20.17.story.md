# Story 20.17 — Pedidos: status manual reverte + faturado não troca de aba

| Campo | Valor |
|-------|-------|
| **Epic** | Epic 20 — Ajustes Financeiro GDP |
| **Status** | Done (gate PASS — fix em produção + realtime verificado, 2026-06-19) |
| **Prioridade** | ALTA (P1) |
| **Estimativa** | M (média — mexe em realtime/reconciliação; risco médio) |
| **Executor** | @dev (Dex) |
| **Quality Gate** | @qa (Quinn) |
| **Encaminhamento** | Recomendado @architect validar a estratégia de reconciliação antes do @dev (toca realtime sync) |
| **Origem** | Handoff @analyst (Atlas) — `handoff-analyst-to-pm-4bugs-20260618.yaml` (PROB-2) |
| **Reportado por** | Usuário (produção) — módulo GDP > Pedidos |
| **Data** | 2026-06-18 |

## Problema

Dois sintomas relacionados no módulo de Pedidos:

1. **Ao faturar um pedido** (emitir NF-e), ele **não muda automaticamente para a aba de status correta** ("Faturado") — o usuário tem a percepção de que "sumiu".
2. **Ao alterar o status manualmente**, o pedido **volta sozinho para o status anterior** automaticamente, impedindo a correção do status.

## Causa Raiz (evidência de código — @analyst)

### Sintoma 1 — Não troca de aba ao faturar
`gerarNotaFiscalPedido()` em `gdp-notas-fiscais.js:1125` **seta corretamente** `pedido.status = "faturado"` e chama `savePedidos()` + `renderAll()`. Porém a aba ativa (`pedidoStatusTabAtual`) **não navega** — se o usuário está na aba "Em Aberto", o filtro de render (`gdp-pedidos.js:213`) exclui o pedido agora "faturado" e ele desaparece da visão atual, em vez de seguir o usuário.

### Sintoma 2 — Status manual reverte (race condition de realtime)
- `savePedidos()` (`gdp-core.js:1389-1398`) persiste **cada pedido async individualmente** no Supabase (`gdpApi.pedidos.save(p)`).
- `gdp-api.js:281` seta `item.updated_at = new Date().toISOString()` no momento do save.
- Quando o usuário edita o status manualmente, **saves ainda em voo** (de uma operação anterior) chegam de volta como eventos `postgres_changes` **depois** da edição, com `updated_at` mais novo.
- O handler de realtime `gdp-realtime.js:196-217` faz **substituição wholesale** do item local pelo registro remoto quando o remoto é "mais novo", **sobrescrevendo a edição manual**.
- A "dirty window" de 5s (`gdp-realtime.js:172-176`) não protege quando há **múltiplos saves em voo**.

## Princípio da Correção

1. **Reconciliação de realtime:** não sobrescrever uma edição local mais recente do usuário com um evento de save antigo em voo. A edição manual do usuário deve vencer eventos de eco do próprio cliente. (Estratégia a validar com @architect — ver Story 20.15b, que tem design correlato de origem `user` vs `system`.)
2. **UX de faturar:** ao faturar com sucesso, **navegar automaticamente para a aba "Faturado"** e renderizar o pedido lá (decisão do stakeholder).

## Acceptance Criteria (Given/When/Then)

### AC1 — Status manual NÃO reverte
```
DADO um pedido em qualquer status
QUANDO o usuário altera o status manualmente (alterarStatusPedido ou aplicarSituacaoBulk)
ENTÃO o pedido permanece no novo status
  E NÃO retorna automaticamente ao status anterior por eco de realtime
```

### AC2 — Faturar navega para a aba "Faturado"
```
DADO um pedido sendo faturado (emissão de NF-e com sucesso)
QUANDO o faturamento conclui
ENTÃO o status vira "faturado"
  E a aba ativa muda automaticamente para "Faturado"
  E o pedido aparece visível nessa aba (não "some")
```

### AC3 — Edição manual logo após faturar é respeitada
```
DADO um pedido recém-faturado
QUANDO o usuário corrige o status manualmente em seguida
ENTÃO a correção persiste e não é revertida por save em voo do faturamento
```

### AC4 — Realtime de OUTRO usuário ainda atualiza
```
DADO 2 usuários na mesma empresa
QUANDO o usuário B altera o status de um pedido
ENTÃO o usuário A vê a atualização (a proteção é só contra ECO do próprio cliente, não contra mudança legítima remota)
```

### AC5 — Sem perda de outras edições concorrentes
```
DADO saves de campos distintos do mesmo pedido em sequência
QUANDO os ecos de realtime chegam
ENTÃO nenhuma edição legítima do usuário é perdida (reconciliação por campo/timestamp, não wholesale cego)
```

### AC6 — Bump de versão dos JS alterados no HTML

## Escopo

**IN:**
- Reconciliação de realtime que não reverte edição manual local recente (`gdp-realtime.js`).
- Navegação automática para a aba "Faturado" ao faturar (`gdp-notas-fiscais.js` + `gdp-pedidos.js`).
- Garantir `updated_at`/marcação de origem coerente no fluxo de save (`gdp-core.js`/`gdp-api.js`).

**OUT:**
- Recalibração geral das regras de sync local↔nuvem (Story 20.15b).
- Fluxo de emissão NF-e em si (Story 20.14, já entregue).

## Arquivos Envolvidos

| Arquivo | Local aprox. | Ação |
|---------|--------------|------|
| `js/gdp-realtime.js` | 167-217 | Reconciliação: não sobrescrever edição local mais recente; tratar eco do próprio cliente |
| `js/gdp-notas-fiscais.js` | 1117-1126 | Após faturar: `setPedidoStatusTab('faturado')` |
| `js/gdp-pedidos.js` | 199/213 (render/filter), 3199 (`alterarStatusPedido`), 737 (`aplicarSituacaoBulk`), 3074 (`renderPedidosStatusTabs`) | Suporte à navegação de aba + garantir persistência do status |
| `js/gdp-core.js` | 1389-1398 (`savePedidos`) | Coerência de timestamp/origem do save |
| `js/gdp-api.js` | 281 | `updated_at` |
| `gdp-contratos.html` | tags script | Bump `?v=N` |

## Riscos

- **MÉDIO.** Toca realtime/reconciliação em produção. Risco de regressão: bloquear updates legítimos de outros usuários (AC4) se a proteção contra eco for ampla demais.
- Recomendado @architect validar a estratégia (correlato à Story 20.15b — reutilizar conceito `origin: user|system`).
- Teste em 2 navegadores obrigatório (edição local não reverte; update remoto legítimo aplica).

## Teste

1. **AC1:** alterar status manual → aguardar > janela de eco → status permanece.
2. **AC2:** faturar pedido → aba muda para "Faturado" e pedido aparece.
3. **AC3:** faturar e logo corrigir status → correção persiste.
4. **AC4:** 2 navegadores — B muda status, A vê a mudança.
5. **AC5:** edições sequenciais de campos distintos → nenhuma se perde.

## File List

- [x] `painel-caixa-escolar/squads/caixa-escolar/dashboard/gdp-api.js` (echo buffer + `gdpApiIsSelfEcho` + `_markSelfEcho` no save; v15→16)
- [x] `painel-caixa-escolar/squads/caixa-escolar/dashboard/js/gdp-realtime.js` (guard echo suppression no UPDATE v3→4; + fix CONCERNS: ramo não-dirty condicionado ao timestamp v4→5)
- [x] `painel-caixa-escolar/squads/caixa-escolar/dashboard/js/gdp-core.js` (`savePedidos(changedId)` seletivo retrocompat; v31→32)
- [x] `painel-caixa-escolar/squads/caixa-escolar/dashboard/js/gdp-pedidos.js` (`alterarStatusPedido` save seletivo; v23→24)
- [x] `painel-caixa-escolar/squads/caixa-escolar/dashboard/js/gdp-notas-fiscais.js` (`setPedidoStatusTab('faturado')` + save seletivo ao faturar; v18→19)
- [x] `painel-caixa-escolar/squads/caixa-escolar/dashboard/gdp-contratos.html` (bump dos 5 JS)

## Dev Agent Record

**Agent Model Used:** claude-opus-4-8[1m]

**Completion Notes (implementado conforme `docs/architecture/20.17-realtime-status-reconciliation-design.md`):**
- **Camada 1 (echo suppression):** `gdp-api.js` — buffer `_selfEchoes` (TTL 8s), assinatura = `updated_at|status` (ambos snake_case, consistentes com o record raw do postgres_changes). `_markSelfEcho` chamado no `save()` após mapToTable. `window.gdpApiIsSelfEcho` exposto.
- **Camada 1b (guard):** `gdp-realtime.js` — no ramo UPDATE, early-return se `gdpApiIsSelfEcho` bater, ANTES de qualquer escrita (dirty e não-dirty). Lógica de timestamp para updates remotos **inalterada** (AC4/AC5).
- **Camada 2 (save seletivo):** `gdp-core.js` `savePedidos(changedId)` opcional. Só `alterarStatusPedido` (pedidos.js:3204) e faturar (notas-fiscais.js:1126) viraram seletivos; **36 chamadas sem arg intactas** (boot/bulk).
- **Camada 3 (navegação):** `setPedidoStatusTab('faturado')` ao faturar (com guard typeof).
- **Consistência verificada:** `status` e `updated_at` ∈ `TABLE_COLS.pedidos` → mapToTable preserva → assinatura bate com o record do realtime.
- **Testes (lógica):** 5 cenários de echo suppression PASS (AC1 eco suprimido, AC4 update remoto NÃO suprimido, AC3 correção pós-faturar não confundida, TTL 8s, registro alheio). Save seletivo: sem arg salva todos, com arg salva 1. `node --check` OK nos 5 JS.
- **CodeRabbit:** indisponível (WSL) — autorrevisão manual.
- **Pendente:** validação obrigatória em **2 navegadores** (AC4) — só o @qa/usuário em produção consegue. Recomendado @qa full.

## 🤖 CodeRabbit Integration

- **Story Type:** Bug Fix (realtime/race condition) — alta criticidade
- **Primary Agent:** @dev
- **Focus Areas:** race condition, reconciliação de estado, não-regressão de sync multiusuário
- **Quality Gate:** @qa (full mode) — teste em 2 navegadores obrigatório
- **Self-Healing:** light (2 iterações, CRITICAL only)

## Change Log

- 2026-06-18 — @pm (Morgan) story criada a partir do handoff @analyst (PROB-2). Dois sintomas: não-troca-de-aba ao faturar (UI) + reversão de status manual (race condition de realtime). Decisão do stakeholder: faturar deve navegar para aba "Faturado". Encaminhamento: @architect validar estratégia de reconciliação (correlato 20.15b). Status: Draft.
- 2026-06-18 — @po (Pax) validação: **GO (score 10/10)**. `file:line` conferidos (notas-fiscais.js:1125 status=faturado; pedidos.js:3055 setPedidoStatusTab existe; realtime.js:199 substituição wholesale). ACs cobrem ambos os sintomas + anti-regressão multiusuário (AC4/AC5). **Condição forte (não bloqueante):** por tocar realtime/reconciliação (risco médio), recomendado passar pelo @architect antes do @dev, reusando o conceito `origin: user|system` da 20.15b. Status Draft → Ready.
- 2026-06-18 — @architect (Aria) análise arquitetural: produzido `docs/architecture/20.17-realtime-status-reconciliation-design.md`. **Causa-raiz refinada:** 3 defeitos compostos — (1) `savePedidos()` re-salva os 145 pedidos por clique (gdp-core.js:1396); (2) eco do próprio cliente indistinguível de update remoto; (3) `updated_at` gerado no upsert, não na edição. **Solução em 3 camadas:** echo suppression determinística (assinatura por `updated_at` em gdp-api.js + guard em gdp-realtime.js), save seletivo `savePedidos(changedId)` retrocompat, e `setPedidoStatusTab('faturado')` ao faturar. Auditoria: 38 chamadas de savePedidos — só 2 viram seletivas (resto intacto). Ordem em 6 commits; passos 1+2 já resolvem AC1/AC3. Complexidade STANDARD-COMPLEX (14). Pronta para @dev (+ @qa full, teste 2 navegadores obrigatório).
- 2026-06-18 — @dev (Dex) implementação completa dos 6 passos do design. Echo suppression (gdp-api.js + gdp-realtime.js), save seletivo (gdp-core.js + 2 handlers), navegação de aba ao faturar. Assinatura snake_case consistente verificada. 5 cenários de echo suppression testados (PASS), regressão do save seletivo OK (36 chamadas sem arg intactas). Bump dos 5 JS. `node --check` OK. **Pendente: validação em 2 navegadores (AC4) — @qa full.** Status → Ready for Review.

## QA Results

**Revisor:** @qa (Quinn) · **Data:** 2026-06-18 · **Veredito: CONCERNS** · Gate: `docs/qa/gates/20.17-status-reverte-realtime.yml`

**Método:** revisão de código + **simulação da race condition** com as funções reais extraídas do arquivo (CodeRabbit indisponível). Teste 2-navegadores **pendente** (código não deployado — realtime exige o código novo nos 2 clientes).

| Check | Resultado |
|-------|-----------|
| Camada 1 (echo suppression) | ✅ PASS — `_selfEchoes`/`gdpApiIsSelfEcho` corretos; assinatura `updated_at|status` consistente (snake_case, campos em TABLE_COLS.pedidos). |
| AC1/AC3 (caso comum) | ✅ PASS — 5 cenários de echo suppression; edição manual e correção pós-faturar preservadas. |
| AC4 (multiusuário) por construção | ✅ PASS (lógica) — `_selfEchoes` é por-cliente (IIFE); browser B não tem o eco do A → update remoto não é suprimido. **Falta validar em 2 navegadores pós-deploy.** |
| AC2 (navegar p/ aba) | ✅ PASS (código) — `setPedidoStatusTab('faturado')` com guard. **Falta validar visualmente pós-deploy.** |
| Save seletivo (retrocompat) | ✅ PASS — 36 sem arg intactas, 2 seletivas. |
| INSERT não reverte | ✅ PASS — `if (!exists)` protege. |
| **Ramo NÃO-dirty (wholesale cego)** | ⚠️ **CONCERNS** |

**🔴 Achado (CONCERNS, medium):** O ramo **não-dirty** de `gdp-realtime.js:214-225` mantém a **substituição wholesale cega** (`items[j]=record`). A simulação prova um edge case real: um **eco atrasado >5s** de um save antigo do mesmo pedido (cuja entrada de echo já foi sobrescrita por um save mais novo) **não é suprimido** e **sobrescreve cegamente** → **reverte o status**. Isto casa exatamente com o relato intermitente do usuário ("muitas vezes... volta o pedido pro status anterior").

O @architect previu isto na seção 4.2 do design como "recomendação adicional (defensiva)" — a simulação mostra que **não é opcional** para fechar o caso. **Fix de ~3 linhas:** condicionar o ramo não-dirty ao timestamp (igual ao dirty):
```js
// gdp-realtime.js, ramo não-dirty (L216-222):
var rTs = record.updated_at || record.updatedAt || '';
var lTs = items[j].updated_at || items[j].updatedAt || '';
if (!lTs || !rTs || rTs >= lTs) { items[j] = record; changed = true; }
```

**🟠 Achado (high, requirements):** AC2/AC4 **exigem teste em 2 navegadores** e não foi possível (código não deployado). **Gate pós-deploy obrigatório.**

**Decisão:** CONCERNS — núcleo correto (AC1/AC2/AC3 caso comum). Recomendo: (1) @dev aplicar o fix de 3 linhas do ramo não-dirty ANTES do deploy; (2) após deploy, teste 2-navegadores obrigatório (AC2/AC4/AC5) antes de Done. Não é FAIL porque a base é sólida e os fixes são pequenos e precisos.

— Quinn, guardião da qualidade 🛡️

---

- 2026-06-18 — @dev (Dex) **fix do CONCERNS**: aplicado o fix de timestamp no ramo não-dirty de `gdp-realtime.js` (L214+). Agora a sobrescrita só ocorre se `rTs >= lTs` (remoto >= local). Re-validado com a simulação do @qa: (1) eco atrasado >5s NÃO reverte mais o status ✅; (2) update legítimo mais novo de outro usuário AINDA aplica (AC4 sem regressão) ✅. Bump gdp-realtime v4→5. `node --check` OK. **Pendente: teste obrigatório em 2 navegadores pós-deploy (AC2/AC4/AC5).** Status mantém Ready for Review.
