# Story 4.83 — Eliminar sync_data legacy do boot — reduzir de 30s para ~2s

- **Epic:** EPIC-4 — GDP Caixa Escolar
- **Status:** Ready for Review
- **Complexity:** S (Small)
- **Agent:** @dev
- **Depends on:** Story 4.80 (otimizações de boot)
- **Priority:** Critical (sistema inutilizável — 30s para carregar)

## Descrição

O boot do dashboard e portal estava levando ~30 segundos. Diagnóstico profundo revelou que **98.5% do tempo** era causado por uma única chamada legacy à tabela `sync_data` do Supabase (`syncFromCloud()` → `cloudLoadAll()` → `GET /sync_data?user_id=eq.LARIUCCI`).

As queries diretas às tabelas reais do Supabase (`contratos`, `pedidos`, etc. via `gdpApi`) levam apenas **100-400ms cada** em paralelo. O plano gratuito do Supabase **NÃO é o problema** — o problema era a arquitetura legacy de sync.

### Medições antes do fix

| Componente | Tempo | % do boot |
|-----------|-------|-----------|
| `syncFromCloud()` → sync_data GET | 29.6s | 98.5% |
| 24x `cloudSave()` POST sync_data | 6.9s | overlapping |
| `/api/db-migrate` cold start | 10.4s | overlapping |
| 6x queries tabelas reais (paralelo) | 0.4s | 1.3% |
| JS files loading | 0.6s | 0.2% |

### O que foi eliminado

1. `syncFromCloud()` do boot em `gdp-init.js` — não mais chamado durante inicialização
2. `syncFromCloud()` e `syncToCloud()` do init em `gdp-portal.html`
3. `syncFromCloud()` e `syncToCloud()` do boot em `app.js`
4. `readSyncData()` fallback no `gdp-api.js` `list()` — retorna `[]` em vez de consultar sync_data
5. Boot flag timeout reduzido de 10s para 3s

### O que foi preservado

- `syncFromCloud()` ainda existe para uso manual (`forcarSyncCompleto`)
- `cloudSave()` ainda funciona para saves de dados auxiliares (config, estoque-intel) — só bloqueia durante boot
- Tabelas reais Supabase continuam carregando normalmente via `gdpApi`

## Acceptance Criteria

### AC1: Boot em menos de 5 segundos
- **Given** o dashboard é aberto
- **When** a página carrega
- **Then** o DOM fica interativo em menos de 5 segundos
- **And** dados das tabelas Supabase são exibidos

### AC2: Nenhuma chamada sync_data no boot
- **Given** o dashboard inicia
- **When** o boot é executado
- **Then** nenhuma request é feita para `/rest/v1/sync_data` durante o boot
- **And** todas as queries vão para as tabelas reais (contratos, pedidos, etc.)

### AC3: Portal escolar rápido
- **Given** o portal escolar é aberto
- **When** a página inicia
- **Then** não há chamada para syncFromCloud/syncToCloud
- **And** login e contratos carregam em menos de 5 segundos

## File List

- [x] `painel-caixa-escolar/squads/caixa-escolar/dashboard/js/gdp-init.js`
- [x] `painel-caixa-escolar/squads/caixa-escolar/dashboard/gdp-api.js`
- [x] `painel-caixa-escolar/squads/caixa-escolar/dashboard/gdp-portal.html`
- [x] `painel-caixa-escolar/squads/caixa-escolar/dashboard/app.js`

## Change Log

| Date | Agent | Change |
|------|-------|--------|
| 2026-05-26 | @analyst | Diagnóstico profundo: sync_data = 29.6s (98.5% do boot). Supabase free tier NÃO é o problema |
| 2026-05-26 | @dev | Eliminado syncFromCloud do boot (gdp-init.js, portal, app.js), readSyncData fallback removido, boot flag 10s→3s |
