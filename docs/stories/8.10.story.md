# Story 8.10 — Migration 006 — Indices GIN e Funcoes RPC de Agregacao

**Epic:** Epic 8 — Consolidacao GDP e Fechamento de Gaps Operacionais
**Status:** Ready
**Priority:** P2
**Story Points:** 2

---

## Descricao

Com os dados fluindo para `preco_historico` (Story 8.9), esta story cria os indices e funcoes RPC no Supabase necessarios para queries de agregacao performantes. Inclui indice GIN para queries JSONB (metadata->municipio, metadata->grupo_despesa), indice composto para queries temporais por SKU, e duas funcoes RPC: `preco_medio_por_municipio()` e `tendencia_preco_mensal()`.

**Referencia SOP:** Etapa 14 — Serie Historica (analytics por cidade/SRE/escola)

---

## Criterios de Aceitacao

- [ ] AC-1: Migration `006_preco_historico_enhancements.sql` criada e aplicada com sucesso
- [ ] AC-2: Indice GIN `idx_preco_hist_metadata` criado em `preco_historico.metadata`
- [ ] AC-3: Indice composto `idx_preco_hist_sku_created` criado em `(sku, created_at DESC)`
- [ ] AC-4: Funcao RPC `preco_medio_por_municipio(p_empresa_id, p_meses)` retorna dados corretos: SKU, municipio, preco_medio, preco_min, preco_max, total_registros, total_ganhos, total_perdidos, taxa_conversao
- [ ] AC-5: Funcao RPC `tendencia_preco_mensal(p_empresa_id, p_sku, p_meses)` retorna dados corretos: SKU, mes, preco_medio, margem_media, total_registros
- [ ] AC-6: Funcoes RPC usam SECURITY INVOKER (nao DEFINER) para respeitar RLS existente (ISS-2 do Critique)
- [ ] AC-7: Funcoes RPC suportam paginacao via parametros (NFR-G4-002)
- [ ] AC-8: Chamadas via `supabase.rpc('preco_medio_por_municipio', {...})` retornam dados filtrados por empresa autenticada

---

## Notas Tecnicas

### Migration 006

Referencia completa na spec SPEC-GDP-GAPS.md secao 6.5. Ajustes necessarios com base no ISS-2 do Critique:

```sql
-- Migration: 006_preco_historico_enhancements.sql

-- Indice GIN para queries JSONB (metadata->>'municipio', metadata->>'grupo_despesa')
CREATE INDEX IF NOT EXISTS idx_preco_hist_metadata
  ON preco_historico USING GIN (metadata);

-- Indice para queries temporais por SKU
CREATE INDEX IF NOT EXISTS idx_preco_hist_sku_created
  ON preco_historico(sku, created_at DESC);

-- Funcao RPC: preco medio por municipio
-- NOTA: Usando SECURITY INVOKER (nao DEFINER) para respeitar RLS
CREATE OR REPLACE FUNCTION preco_medio_por_municipio(
  p_empresa_id TEXT,
  p_meses INTEGER DEFAULT 6
)
RETURNS TABLE (
  sku TEXT,
  municipio TEXT,
  preco_medio NUMERIC,
  preco_min NUMERIC,
  preco_max NUMERIC,
  total_registros BIGINT,
  total_ganhos BIGINT,
  total_perdidos BIGINT,
  taxa_conversao NUMERIC
) AS $$
BEGIN
  RETURN QUERY
  SELECT
    ph.sku,
    ph.metadata->>'municipio' as municipio,
    ROUND(AVG(ph.valor), 2) as preco_medio,
    MIN(ph.valor) as preco_min,
    MAX(ph.valor) as preco_max,
    COUNT(*)::BIGINT as total_registros,
    COUNT(*) FILTER (WHERE ph.tipo = 'ganho')::BIGINT as total_ganhos,
    COUNT(*) FILTER (WHERE ph.tipo = 'perdido')::BIGINT as total_perdidos,
    CASE
      WHEN COUNT(*) FILTER (WHERE ph.tipo IN ('ganho','perdido')) > 0
      THEN ROUND(
        COUNT(*) FILTER (WHERE ph.tipo = 'ganho')::NUMERIC /
        COUNT(*) FILTER (WHERE ph.tipo IN ('ganho','perdido'))::NUMERIC * 100,
        1
      )
      ELSE NULL
    END as taxa_conversao
  FROM preco_historico ph
  WHERE ph.empresa_id = p_empresa_id
    AND ph.created_at >= NOW() - (p_meses || ' months')::INTERVAL
  GROUP BY ph.sku, ph.metadata->>'municipio'
  ORDER BY ph.sku, municipio;
END;
$$ LANGUAGE plpgsql SECURITY INVOKER;

-- Funcao RPC: tendencia de preco mensal
CREATE OR REPLACE FUNCTION tendencia_preco_mensal(
  p_empresa_id TEXT,
  p_sku TEXT DEFAULT NULL,
  p_meses INTEGER DEFAULT 12
)
RETURNS TABLE (
  sku TEXT,
  mes DATE,
  preco_medio NUMERIC,
  margem_media NUMERIC,
  total_registros BIGINT
) AS $$
BEGIN
  RETURN QUERY
  SELECT
    ph.sku,
    DATE_TRUNC('month', ph.created_at)::DATE as mes,
    ROUND(AVG(ph.valor), 2) as preco_medio,
    ROUND(AVG(ph.margem_pct), 1) as margem_media,
    COUNT(*)::BIGINT as total_registros
  FROM preco_historico ph
  WHERE ph.empresa_id = p_empresa_id
    AND (p_sku IS NULL OR ph.sku = p_sku)
    AND ph.tipo IN ('proposta', 'ganho')
    AND ph.created_at >= NOW() - (p_meses || ' months')::INTERVAL
  GROUP BY ph.sku, DATE_TRUNC('month', ph.created_at)
  ORDER BY ph.sku, mes;
END;
$$ LANGUAGE plpgsql SECURITY INVOKER;
```

### Diferenca da Spec (ISS-2 Resolvido)

A spec original usava `SECURITY DEFINER`, que bypassa RLS. Por recomendacao do QA Critique (ISS-2), usamos `SECURITY INVOKER` para que as funcoes respeitem as policies RLS ja configuradas na tabela. O parametro `p_empresa_id` serve apenas como filtro adicional, nao como unico mecanismo de seguranca.

### Chamada Client-Side

```javascript
// Preco medio por municipio (ultimos 6 meses)
const { data, error } = await supabase.rpc('preco_medio_por_municipio', {
  p_empresa_id: empresaAtual.id,
  p_meses: 6,
});

// Tendencia mensal para SKU especifico
const { data, error } = await supabase.rpc('tendencia_preco_mensal', {
  p_empresa_id: empresaAtual.id,
  p_sku: 'BANK-PAPEL-A4-001',
  p_meses: 12,
});
```

---

## Requisitos Relacionados

- FR-G4-003: View de Agregacao por Municipio
- FR-G4-004: View de Agregacao por SRE
- FR-G4-005: View de Tendencia Temporal
- NFR-G4-002: Queries Paginadas
- NFR-G4-004: Indice para metadata->municipio
- CON-G4-001: RLS respeitado nas queries

---

## Escopo

### Inclui
- Migration `006_preco_historico_enhancements.sql`
- Indices GIN e composto
- Funcao RPC `preco_medio_por_municipio()`
- Funcao RPC `tendencia_preco_mensal()`
- Uso de SECURITY INVOKER (resolvendo ISS-2)
- Verificacao de funcionamento via chamada client-side

### Nao Inclui
- Dashboard/UI de serie historica (Story 8.11)
- Funcao RPC por SRE (pode usar mesma funcao com filtro metadata->>'sre')
- Materialized views (nao suportadas no free tier Supabase)

---

## Riscos

| Risco | Probabilidade | Mitigacao |
|-------|:-------------:|-----------|
| Performance de funcoes RPC com volume alto de dados | Baixa | Indices GIN e composto otimizam; parametro `p_meses` limita range |
| SECURITY INVOKER mais lento que DEFINER | Baixa | Diferenca minima; seguranca justifica tradeoff |
| Migration falha se indices ja existem | Baixa | `IF NOT EXISTS` em todos os CREATE INDEX; `CREATE OR REPLACE` em funcoes |

---

## Estimativa

| Atividade | Horas |
|-----------|:-----:|
| Criar migration SQL com indices e funcoes | 1h |
| Aplicar migration no Supabase | 0.5h |
| Testar funcoes RPC via `supabase.rpc()` | 1h |
| Verificar RLS funciona com SECURITY INVOKER | 0.5h |
| Documentar schema atualizado | 0.5h |
| **Total** | **3.5h** |

---

## Dependencias

- **Bloqueado por:** Story 8.9 (Dados precisam existir em `preco_historico` para testar funcoes)
- **Bloqueia:** Story 8.11 (Dashboard depende das funcoes RPC para obter dados agregados)

---

## File List

| Arquivo | Acao |
|---------|------|
| `supabase/migrations/006_preco_historico_enhancements.sql` | Criar — indices e funcoes RPC |

---

*Story criada por @sm (River) — SPEC-GDP-GAPS v1.0, secao 6 (G4)*
*Requisitos: FR-G4-003, FR-G4-004, FR-G4-005, NFR-G4-002, NFR-G4-004, CON-G4-001*
*ISS-2 do Critique resolvido: SECURITY INVOKER em vez de SECURITY DEFINER*
