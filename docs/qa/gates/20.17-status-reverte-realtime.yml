storyId: "20.17"
title: "Pedidos: status manual reverte + faturado não troca de aba"
reviewer: "@qa (Quinn)"
date: "2026-06-18"
verdict: PASS
verdict_history:
  - date: "2026-06-18"
    verdict: CONCERNS
    reason: "Fix do ramo não-dirty (medium) pendente + teste 2-navegadores (high) impossível sem deploy."
  - date: "2026-06-19"
    verdict: PASS
    reason: >
      Re-gate: (1) Issue medium FECHADA — fix do ramo não-dirty confirmado em produção
      (gdp-realtime.js:217-226 condiciona items[j]=record ao timestamp rTs>=lTs; gdp-realtime v5 em prod==GitHub).
      (2) Issue high MITIGADA por evidência de produção (Playwright, login real): realtime ATIVO
      (indicador '🟢 Sincronizado (realtime)' em todas as telas), painel carrega dados reais (155 pedidos),
      AC2 verificável (aba Pedidos com coluna 'Emissao' / setPedidoStatusTab no código). O teste destrutivo
      de AC4 (faturar pedido real → emitir NF-e na SEFAZ) NÃO foi executado por decisão do stakeholder
      (2026-06-19): não emitir NF de teste em produção. AC4 fica validável no uso normal; o código que o
      garante (suppression por-cliente + guard de timestamp) está revisado e em produção.
      Decisão: PASS por evidência de código + produção, com AC4 ao-vivo como verificação de uso normal (não bloqueante)."
method: "Revisão manual + simulação da race condition + verificação em produção (Playwright login real). Teste destrutivo AC4 (NF real) não executado por decisão do stakeholder."
passed:
  - "Camada 1 (echo suppression): gdp-api.js _selfEchoes + gdpApiIsSelfEcho corretos. Assinatura updated_at|status consistente (snake_case ambos os lados, status/updated_at em TABLE_COLS.pedidos)."
  - "AC1/AC3 caso comum: eco do próprio save suprimido (5 cenários PASS). Edição manual e correção pós-faturar preservadas."
  - "AC4 por construção: _selfEchoes é por-cliente (dentro da IIFE) — browser B nunca tem o eco do A → update remoto legítimo NÃO é suprimido."
  - "AC2: setPedidoStatusTab('faturado') ao faturar (guard typeof)."
  - "Save seletivo: retrocompat OK (36 chamadas sem arg salvam todos; 2 seletivas). node --check OK."
  - "INSERT não reverte: guard `if (!exists)` impede sobrescrita via INSERT."
issues:
  - severity: medium
    status: RESOLVED
    resolved_date: "2026-06-19"
    category: code
    ac: "AC1/AC5 (edge case)"
    description: "Caminho NÃO-dirty (gdp-realtime.js:214-225) mantinha substituição wholesale CEGA (items[j]=record). RESOLVIDO: agora condiciona ao timestamp (L221-226: rTs/lTs, só aplica se rTs>=lTs). Confirmado em produção (gdp-realtime v5 prod==GitHub)."
    evidence: "Read gdp-realtime.js:217-226 — guard de timestamp aplicado, com comentário 'Story 20.17'. Codepoint/linha conferidos nesta sessão."
  - severity: high
    status: MITIGATED
    category: requirements
    ac: "AC2, AC4"
    description: "Teste 2-navegadores: AC2 e realtime verificados em produção via Playwright (login real) — realtime ATIVO, painel com dados reais, coluna 'Emissao'. AC4 ao-vivo (faturar→NF real) não executado por decisão do stakeholder (não emitir NF de teste em produção). Código que garante AC4 revisado e em produção."
    recommendation: "Validar AC4 no uso normal: ao faturar um pedido real, confirmar que o status não reverte intermitentemente e que o 2º navegador reflete a mudança. Não bloqueante."
deploy_recommendation: "Já deployado (gdp-realtime v5 em produção == GitHub). Nada pendente."
decision: "PASS — núcleo correto (AC1/AC2/AC3), fix do edge case (medium) RESOLVIDO e em produção, realtime verificado ao vivo. AC4 destrutivo fica como verificação de uso normal por decisão do stakeholder."
