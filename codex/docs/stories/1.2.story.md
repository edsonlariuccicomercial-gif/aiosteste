# Story 1.2 - Espelho SGD com Filtro por SRE e Objeto

## Status
Done

## Objetivo
Construir o primeiro fluxo funcional do produto: coletar cotações do SGD em painel proprio e permitir filtro por SRE, municipio e objeto.

## Criterios de Aceite

- [x] Login assistido no SGD funcional para coleta.
- [x] Tabela de cotações espelhada com colunas: ID, Ano, Escola, Prazo, Status.
- [x] Escola mapeada para SRE e municipio.
- [x] Busca por objeto com classificacao inicial.
- [x] Filtros combinados funcionando (SRE + municipio + objeto + status + prazo).
- [x] Exportacao CSV da lista filtrada.
- [x] Log de execucao da coleta salvo.

## Tarefas

- [x] Criar modelo de dados `cotacoes`, `escolas`, `sres`, `objetos`.
- [x] Implementar coletor base da rota `/compras/orcamentos`.
- [x] Construir mapeamento escola -> SRE (seed inicial).
- [x] Implementar classificador simples de objeto (keywords).
- [x] Criar tela de listagem com filtros e busca.
- [x] Criar exportacao CSV.
- [x] Registrar evidencias de teste com capturas.

## Riscos

- Mudancas de layout no SGD podem quebrar coleta.
- Texto do objeto pode vir inconsistente/insuficiente.

## Mitigacoes

- Versionar seletores de scraping por pagina.
- Logar falhas por etapa e ter retentativa.
- Permitir correcao manual de classificacao do objeto.

## Definition of Done

- [x] Criterios de aceite validados.
- [x] Story atualizada com evidencias.
- [x] Lista de arquivos alterados atualizada.

## Evidencias

- Coleta real do SGD executada com sucesso (`10` cotacoes extraidas).
- Exportacao CSV adicionada na tabela priorizada (`Exportar CSV`) com base na visao filtrada.
- Score de oportunidade (0-100) implementado no dashboard e no CSV (`score_oportunidade`).
- Alertas criticos de prazo adicionados no dashboard (top prazos <= 5 dias por filtro).
- Coletor atualizado com classificacao de objeto por keywords e `confiancaObjeto` (`alta/media/baixa`).
- Mapeamento escola -> municipio/SRE movido para arquivo curavel (`school-territory-map.json`) com `confiancaTerritorio`.
- Score de oportunidade refinado com peso estrategico por tipo de objeto.
- Auditoria automatica de cobertura territorial criada (`dashboard:audit:territory`) com relatorio em JSON e Markdown.
- Acoes rapidas no dashboard adicionadas: `Prioridade Alta`, `Prazo 48h`, `Baixa confianca` e `Exportar Urgentes`.
- Simulador de margem liquida evoluido com base de custo por SKU (`sku-costs.json`), fator regional por SRE e frete percentual.
- Tabela e CSV passaram a incluir `SKU de referencia` e `preco minimo (modelo)` por cotacao, com classificacao automatica por objeto.
- Classificacao `objeto -> SKU` migrada para dicionario curavel (`object-sku-rules.json`) com suporte a sinonimos e acentos.
- Indicador de cobertura SKU no dashboard (`classificadas/total`) e lista dos objetos nao classificados para curadoria rapida.
- Botao `Copiar sugestoes de regras` gera JSON candidato para acelerar atualizacao do `object-sku-rules.json`.
- Auditoria automatica de cobertura SKU criada (`dashboard:audit:sku`) com relatorio em JSON e Markdown.
- Cobertura SKU atual no dataset: `50% (5/10)`; pendencia dominante: `Objeto nao identificado`.
- Coletor SGD reforcado para reduzir `Objeto nao identificado`: extracao de objeto por cabecalho/heuristica e classificacao com texto enriquecido (`rowRaw` + keywords expandidas).
- Coletor agora nao sobrescreve `quotes.json` quando vier `0` cotacoes e salva diagnostico (`.aios/sgd-audit-logged/collect-empty.png` e `.html`).
- Scripts de auditoria/colecao ajustados para ler JSON com BOM (robustez operacional em ambientes Windows).
- Extracao por modal `Visualizar` implementada: leitura de `Detalhamento` + `Lista de Itens Solicitados` para classificar objeto quando a tabela nao expor coluna de objeto.
- Coleta real validada com credencial de fornecedor (`02/03/2026`): `10` cotacoes extraidas e classificacao de objeto em `100% (10/10)`.
- Cobertura SKU atualizada para `100% (10/10)` apos incluir regra/SKU de `Servicos e eventos escolares`.
- Validacao automatica de cobertura SKU adicionada (`dashboard:validate:sku`) com meta minima padrao de `95%`.
- Base de custo da categoria `Servicos e eventos escolares` evoluida com faixas por tipo de evento (`rodizio/buffet`, `excursao`, `formatura`).
- Revalidacao final executada em `02/03/2026`: `npm.cmd test`, `dashboard:audit:territory`, `dashboard:audit:sku` e `dashboard:validate:sku` aprovados.
- Preview desktop: `.aios/sgd-audit-logged/dashboard-live-preview.png`
- Preview mobile: `.aios/sgd-audit-logged/dashboard-preview-mobile.png`

## File List

- `dashboard/index.html`
- `dashboard/styles.css`
- `dashboard/app.js`
- `dashboard/data/quotes.json`
- `dashboard/data/sku-costs.json`
- `dashboard/data/object-sku-rules.json`
- `dashboard/data/school-territory-map.json`
- `dashboard/data/territory-coverage-report.json`
- `scripts/collect-sgd-orcamentos.js`
- `scripts/audit-territory-coverage.js`
- `scripts/audit-sku-coverage.js`
- `docs/ops/territory-coverage-report.md`
- `docs/ops/sku-coverage-report.md`
- `scripts/serve-dashboard.js`
- `package.json`
